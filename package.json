{
  "name": "skeleton-app",
  "version": "0.0.0",
  "description": "to serve as a starting point for node apps",
  "main": "index.js",
  "scripts": {
    "start": "node app/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint app/**/*.{js,ts,json}",
    "lint:fix": "eslint --fix app/**/*.{js,ts,json}"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.19.0"
  }
}
